name: Test Workflow

on:
  push:
    branches:
      - new-branch/first

jobs:
  test_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Workflow triggered successfully"
