name: Work_flow file
on: 
   issues: [deleted]
   workflow_dispatch:
jobs:
  first_job:
    name: First job
    runs-on: ubuntu-latest
    steps:
      - name: Signle line run step
        run: echo testing in step1
      - name: Multi line run step
        run: |
              echo testing step2
              echo testing step2 line2
   second_job:
      name: Second job
      runs-on: ubuntu-latest
      steps:
         - name: Second job step1
           run: echo second job step1 
   Third_job:
      name: Third job
      runs-on: ubuntu-latest
      needs: second_job
      steps:
         - name: Third job step1
           run: echo second job step1 
      
      
 
