name: Work_flow file
on:
  push:
    branches:
      - new-branch/first
      - main
  workflow_dispatch:
jobs:
  name: first
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Single line run step
        run: echo testing in step1

      - name: Multi line run step
        run: |
          echo testing step2
          echo testing step2 line2

  second_job:
    name: second
    runs-on: ubuntu-latest
    steps:
      - name: Second job step1
        run: echo second job step1

  third_job:
    name: third
    runs-on: ubuntu-latest
    needs: second_job
    steps:
      - name: Third job step1
        run: echo second job step1

  fourth_job:
    name: web_page
    if: github.ref == "new-branch/first" && github.event_name == "push"
    runs-on: ubuntu-latest
    steps:
     - name: code checkout
       uses: actions/checkout@v4

     - name: read file
       run: cat myweb.html 
this is tss


this is happen
dd